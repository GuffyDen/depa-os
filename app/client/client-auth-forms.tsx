"use client";
import { FormEvent,useState } from "react";
import { useRouter,useSearchParams } from "next/navigation";
function Field({label,type="text",value,onChange,autoComplete,inputMode}:{label:string;type?:string;value:string;onChange:(value:string)=>void;autoComplete:string;inputMode?:"text"|"tel"}){return <label className="cp-field"><span>{label}</span><input type={type} value={value} onChange={event=>onChange(event.target.value)} autoComplete={autoComplete} inputMode={inputMode} required/></label>}
export function ClientLoginForm(){const router=useRouter(),[identifier,setIdentifier]=useState(""),[password,setPassword]=useState(""),[show,setShow]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");async function submit(event:FormEvent){event.preventDefault();setBusy(true);setError("");try{const response=await fetch("/api/client/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({identifier,password})}),data=await response.json();if(!response.ok)throw new Error(data.error);router.replace("/client")}catch(value){setError(value instanceof Error?value.message:"Не удалось войти.")}finally{setBusy(false)}}return <form className="cp-auth-card" onSubmit={submit}><span className="cp-kicker">ЛИЧНЫЙ КАБИНЕТ</span><h2>Вход</h2><p>Используйте номер телефона или логин из приглашения.</p><Field label="Логин" value={identifier} onChange={setIdentifier} autoComplete="username" inputMode="tel"/><div className="cp-password"><Field label="Пароль" type={show?"text":"password"} value={password} onChange={setPassword} autoComplete="current-password"/><button type="button" onClick={()=>setShow(value=>!value)}>{show?"Скрыть":"Показать"}</button></div>{error?<div className="cp-alert" role="alert">{error}</div>:null}<button className="cp-primary" disabled={busy}>{busy?"Входим…":"ВОЙТИ"}</button><small>Проблемы со входом? Свяжитесь с менеджером DEPA.</small></form>}
export function ClientActivateForm(){const router=useRouter(),params=useSearchParams(),[password,setPassword]=useState(""),[repeat,setRepeat]=useState(""),[show,setShow]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");async function submit(event:FormEvent){event.preventDefault();setBusy(true);setError("");try{const response=await fetch("/api/client/auth/activate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token:params.get("token"),password,passwordRepeat:repeat})}),data=await response.json();if(!response.ok)throw new Error(data.error);router.replace("/client")}catch(value){setError(value instanceof Error?value.message:"Не удалось создать доступ.")}finally{setBusy(false)}}return <form className="cp-auth-card" onSubmit={submit}><span className="cp-kicker">ПЕРВЫЙ ВХОД</span><h2>Создайте пароль</h2><p>Минимум 8 символов. Пароль хранится только в защищённом виде.</p><div className="cp-password"><Field label="Новый пароль" type={show?"text":"password"} value={password} onChange={setPassword} autoComplete="new-password"/><button type="button" onClick={()=>setShow(value=>!value)}>{show?"Скрыть":"Показать"}</button></div><Field label="Повторите пароль" type={show?"text":"password"} value={repeat} onChange={setRepeat} autoComplete="new-password"/>{error?<div className="cp-alert" role="alert">{error}</div>:null}<button className="cp-primary" disabled={busy}>{busy?"Создаём…":"СОЗДАТЬ ДОСТУП"}</button></form>}
