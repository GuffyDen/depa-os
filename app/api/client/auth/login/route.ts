import { clientSessionCookie, ClientPortalError, loginClientPortal } from "../../../../../lib/client-portal";
import { createRequestLogger } from "../../../../../lib/request-logger";
export const dynamic="force-dynamic";
export async function POST(request:Request){const log=createRequestLogger(request,{route:"/api/client/auth/login",action:"CLIENT_LOGIN",actorType:"ANONYMOUS"});try{const body=await request.json() as Record<string,unknown>,result=await loginClientPortal(String(body.identifier??""),String(body.password??""),request);log.success("CLIENT_AUTH_SUCCESS",{actorType:"CLIENT",actorId:result.user.id,clientId:result.user.clientId,entityId:result.user.id});return log.json({user:result.user},{headers:{"Set-Cookie":clientSessionCookie(result.token,request.url)}})}catch(error){const status=error instanceof ClientPortalError?error.status:500;log.failure("CLIENT_AUTH_FAILURE",error,{errorCode:error instanceof ClientPortalError?`CLIENT_AUTH_${status}`:"CLIENT_AUTH_INTERNAL"});return log.json({error:error instanceof Error?error.message:"Не удалось войти."},{status})}}
