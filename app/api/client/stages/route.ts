import { acceptStageByClient,ClientPortalError,getClientPortalUser,rejectStageByClient } from "../../../../lib/client-portal";
export async function POST(request:Request){const user=await getClientPortalUser(request);if(!user)return Response.json({error:"Требуется авторизация клиента."},{status:401});try{const body=await request.json() as Record<string,unknown>,stageId=String(body.stageId??"");return Response.json(body.action==="ACCEPT"?await acceptStageByClient(user,stageId,String(body.comment??"")):body.action==="REJECT"?await rejectStageByClient(user,stageId,String(body.comment??"")):(()=>{throw new ClientPortalError("Неизвестное действие.")})())}catch(error){return Response.json({error:error instanceof Error?error.message:"Не удалось выполнить действие."},{status:error instanceof ClientPortalError?error.status:500})}}
