import { ApartmentPassportError, getClientPassportAttachments } from "@/lib/apartment-passports";
import { getClientPortalUser } from "@/lib/client-portal";
export async function GET(request:Request){const user=await getClientPortalUser(request);if(!user)return Response.json({error:"Требуется авторизация клиента."},{status:401});const url=new URL(request.url);try{return Response.json(await getClientPassportAttachments(user,url.searchParams.get("projectId")??"",url.searchParams.get("section"),url.searchParams.get("versionId"),url.searchParams.get("cursor"),url.searchParams.get("limit")),{headers:{"Cache-Control":"private, no-store"}})}catch(error){return Response.json({error:error instanceof Error?error.message:"Фотографии недоступны."},{status:error instanceof ApartmentPassportError?error.status:500})}}
