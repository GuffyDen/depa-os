import { getRequestUser } from "@/lib/auth";import { createEmployeeHandoverDefect,HandoverError } from "@/lib/handovers";import { AccessError } from "@/lib/permissions";import { createRequestLogger } from "@/lib/request-logger";type Context={params:Promise<{projectId:string}>};const status=(error:unknown)=>error instanceof HandoverError||error instanceof AccessError?error.status:500;
export async function POST(request:Request,{params}:Context){const actor=await getRequestUser(request),projectId=(await params).projectId,log=createRequestLogger(request,{route:"/api/handovers/[projectId]/defects",action:"HANDOVER_DEFECT_CREATE",actorType:actor?"EMPLOYEE":"ANONYMOUS",actorId:actor?.id??null,entityId:projectId});if(!actor)return log.json({error:"Требуется авторизация."},{status:401});let body:Record<string,unknown>;try{body=await request.json() as Record<string,unknown>}catch{return log.json({error:"Некорректные данные."},{status:400})}try{const result=await createEmployeeHandoverDefect(actor,projectId,body);log.success("HANDOVER_DEFECT_CREATE_SUCCESS",{projectId});return log.json(result,{status:201})}catch(error){log.failure("HANDOVER_DEFECT_CREATE_FAILURE",error,{projectId,errorCode:`HANDOVER_${status(error)}`});return log.json({error:error instanceof Error?error.message:"Не удалось создать замечание."},{status:status(error)})}}
