# Local PostgreSQL only. The test guard rejects non-loopback hosts and database
# names that do not begin with depa_os_test.
DATABASE_URL=postgresql://localhost:5432/depa_os_test
DATABASE_DRIVER=pg
NODE_ENV=test

# Test storage is local and must never use a production Vercel Blob token.
TEST_STORAGE_ROOT=.test-storage
